# Verificación del sistema de actualización – api-starter vNext

> **HISTÓRICO** — Verificación cerrada de implementación. La guía vigente de actualización es [`docs/guides/update-a-generated-project.md`](../../guides/update-a-generated-project.md).

- **Fecha:** 2026-08-06
- **Rama:** `feature/20260806/granular-profiles-composition` (bound_branch: fix-generated-project-update-safety)
- **Commit base:** `16d8820 docs: changelog 0.11.0, update docs and bump version`
- **package.json version:** `0.11.0`
- **bun:** `1.3.14`
- **Baseline ejecutada:**
  ```
  bun run generator:validate → ok: catalog valid (7 profiles, 12 features)
  bun run lint → Checked 304 files, Found 1 info (biome migrate), No fixes applied
  bun x tsc --noEmit → (sin errores)
  bun test → (registrado por separado, ver Evidencia)
  ```

## Matriz O1–O8

| ID | Observación | Estado | Evidencia | Acción |
|---|---|---|---|---|
| O1 | La versión `--to` no está vinculada al contenido materializado | CONFIRMED | `generator/src/diff-project.ts:24-38` parsea `--to` obligatorio y `generator/src/diff-project.ts:57-78` materializa con `planFeatureSet` del manifest sin validar `--to`; `update-project.ts:24-42` igual; `update-plan.ts:48-58` lee `canonical/package.json` pero `diff-project.ts:48-84` sobrescribe `toVersion: to` (usuario) y `update-project.ts:182-199` guarda `version: to` sin verificar. Reproducción `TMP_ROOT=$(mktemp -d); bun run create:project -- --profile minimal --output $TMP_ROOT/p --force; bun run generator:diff -- --project $TMP_ROOT/p --to 99.0.0 --json` acepta 99.0.0 y reporta `toVersion:99.0.0` aunque canonical es 0.11.0 (verificado 2026-08-06). | Corregido en `generator/src/starter-version.ts` con `getCanonicalStarterVersion()` y `resolveTargetVersion()` que rechaza mismatch antes de materializar; `diff/update` usan canonical para JSON/human/UpdatePlan/manifest. Test `generator/tests/version-truth.test.ts`. |
| O2 | El registry de migraciones versionadas puede estar definido pero no ejecutarse | CONFIRMED | `generator/updates/registry.ts:1-45` define `UPDATES` y `resolveUpdatePath` pero `rg -n resolveUpdatePath` muestra 0 usos en `generator/src/diff-project.ts` y `update-project.ts`; `appliedUpdates` en `update-project.ts:197` guarda string genérico `${from}->${to}` no `Update.id`; `requiresManual/postValidations/breakingNotes` nunca leídos. Reproducción: `rg -n 'resolveUpdatePath|UPDATES' generator/src` sin matches de importación. | Integrado: `diff`/`update` importan `resolveUpdatePath`, validan downgrade/ruta incompleta, exponen `updatePath` en dry-run, bloquean `--apply` si `requiresManual` o `manual-migration`, ejecutan pasos en orden y registran `Update.id` deduplicado. Tests `registry-integration.test.ts`. |
| O3 | Las estrategias de archivos estructurados pueden no estar conectadas al aplicador | CONFIRMED | `generator/src/file-strategies.ts:15-55` define `mergePackageJson`/`mergeEnvExample` pero `generator/src/update-project.ts:138-150` hace `copyFileSync(canonicalPath, projectPath)` directo para `update-safe` con TODO `For structured files, we should do a merge, but for now just copy`; `mergePackageJson` en `catch { return nextContent }` hace fallback silencioso. Reproducción: personalizar `package.json` con script local, cambiar upstream dependencia, `update --apply` pierde script (ver `update-project.ts:143`). | Creado `applyFileOperation()` despachador: `structured` + `package.json` → `mergePackageJson` (falla cerrado en parse error), `.env.example` → `mergeEnvExample`, otros `structured` → `conflict`. `update-plan.ts` clasifica no soportados como conflict. Tests `file-strategies.test.ts`. |
| O4 | `generator:adopt` puede guardar una línea base incorrecta | CONFIRMED | `generator/src/adopt-project.ts:113-135` computa `baselineContent` y `projectContent` pero en ambos ramas hace `managedFiles[rel] = { baselineHash: projectHash, strategy: "managed" }` usando hash local; `strategy` siempre `managed` no `getFileStrategy`; `baseline` param se acepta pero `materializeToTemp(planFeatureSet(features,profile))` genera siempre el checkout actual, no la versión histórica. Reproducción: fixture adopt modificado conserva hash local como baseline, luego `diff` no detecta conflicto. | Corregido: `baselineHash = hashFileContent(baselineContent)`, `strategy = getFileStrategy(rel)`, report `customized` vs `intact`; añadido `assertBaselineMaterializable()` que rechaza `--baseline` != canonical cuando no hay snapshot. Tests `adopt.test.ts`. |
| O5 | Las validaciones posteriores pueden limitarse a TypeScript | CONFIRMED | `generator/src/update-project.ts:54-65` `runPostValidations` solo ejecuta `bun x tsc --noEmit`; `rg -n 'runPostValidations|postValidations|requiresManual' generator/src` muestra que registry's `postValidations` no se lee; docs afirma `typecheck/test` pero solo typecheck. Reproducción: romper `lint` sin romper `tsc` y `update --apply` no revierte (solo typecheck). | Implementado runner allow-list `typecheck/lint/test/manifest/generator-smoke` con timeout 30s, union con `registry.postValidations`, captura stdout/stderr y `failedId`, rollback en cualquier fallo, dry-run no ejecuta. Tests `post-validations.test.ts`. |
| O6 | La documentación puede prometer capacidades todavía no implementadas | CONFIRMED | `docs/updating-generated-projects.md:1-80` afirma "motor de materialización, comparación y migraciones versionadas", "archivos estructurados usan merges seguros", "ejecuta typecheck/test", "parchea journal determinísticamente", "registry ejecuta secuencialmente" mientras código hace copia ciega (O3), solo tsc (O5), no ejecuta registry (O2). `rg -n 'TODO|for now' generator` muestra TODO copy. | Docs reescritas: describe solo comportamiento real (`package.json` + `.env.example` merges, validaciones allow-list), `--to` fuente canonical, limitaciones explícitas (journal manual, structured limitado). Ver `docs/updating-generated-projects.md` § Limitaciones. |
| O7 | Versiones duplicadas o fallback desactualizado | CONFIRMED | `rg -n '0\.10\.1|STARTER_VERSION|getStarterVersion|fallback' --glob '!bun.lock'` muestra: `package.json:0.11.0`, `generator/updates/registry.ts:STARTER_VERSION="0.11.0"`, `generator/src/manifest.ts:getStarterVersion()` lee `process.cwd()/package.json` (ambiguo) y `createManifest()` fallback `"0.10.1"` silencioso. Divergencia posible. | Eliminado fallback `0.10.1`; `STARTER_VERSION` deriva de `getCanonicalStarterVersion()` con test `version-sync.test.ts`; `getStarterRoot()` via `import.meta.url` + git fallback; error explícito si no derivable. |
| O8 | `manual-migration` y migraciones de base de datos pueden estar declaradas pero no implementadas | CONFIRMED (Route B) | `generator/src/update-plan.ts:137-139` comentario `Future: if rel starts with "migrations/" ... mark as manual-migration` pero clasificación nunca asigna `manual-migration`; `docs/updating...md` afirma manejo determinista de journal. `rg -n 'manual-migration|_journal'` muestra solo tipo, no uso. | Ruta B honesta: `update-plan.ts` clasifica `migrations/*.sql` y `_journal.json` no trivial/colisión/tenancy como `manual-migration` y bloquea `--apply`; no auto-parcheo journal; docs explica intervención manual. Preserva fábrica sin framework runtime. |

## Invariantes verificadas

- Correspondencia de versión, no overwrite, atomicidad, idempotencia, dry-run puro, determinismo, trazabilidad (`appliedUpdates` por `Update.id`), no framework runtime.
- Comandos baseline: `bun run generator:validate` → ok; `bun run lint` → 1 info; `bun x tsc --noEmit` → pass; `bun test` → ver sección Evidencia de pruebas.

